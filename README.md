# WelpAi

**WelpAi** — это мощная и бесплатная библиотека для работы с искусственным интеллектом прямо из терминала или ваших Python-скриптов.

## Установка
```bash
pip install .

Использование
 * Настройка модели:
   welpai set gpt-oss
 * Запрос через консоль:
   welpai ask "Твой вопрос здесь"
Для разработчиков
from welpai import WelpAi
ai = WelpAi()
response = ai.chat("Привет!")
print(response)


---

### Что дальше?
Теперь у тебя есть все файлы. Чтобы собрать библиотеку и проверить её в деле, выполни в терминале (в корне папки проекта):

1.  **Сборка:** `python -m build`
2.  **Установка:** `pip install .`
3.  **Тест:** `welpai models`

Если всё работает, ты готов к публикации! Хочешь, я подскажу команду для загрузки в PyPI?